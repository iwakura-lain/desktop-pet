# Desktop Pet (桌宠)

A lightweight Windows desktop pet built with Unity 6 LTS and URP.
The pet lives on your desktop as a transparent, always-on-top, draggable sprite.

---

## Features

- Transparent borderless window (no taskbar entry)
- Always on top of other windows
- Click & drag to reposition
- Sprite-sheet frame animation
- Idle / Clicked / Dragging states

---

## Project Structure

```
Assets/
  Scripts/
    WindowManager.cs       # Win32 API: transparent window, topmost, drag
    PetController.cs       # State machine: Idle → Clicked → Dragging
    AnimationController.cs # Sprite-sheet frame animator
  Scenes/
    Main.unity             # Scene (must be generated by Unity Editor — see placeholder)
ProjectSettings/
  SETTINGS_README.md       # PlayerSettings guide
.github/
  workflows/
    build.yml              # CI: build on tag v*, release to GitHub Releases
```

---

## Requirements

| Tool | Version |
|---|---|
| Unity Editor | 6 LTS (6000.0.x) |
| Unity module | Windows Build Support (IL2CPP) |
| OS (build host) | Windows 10/11 x64 (or CI via game-ci) |
| .NET | Standard 2.1 |

### Unity Hub setup

1. Install **Unity Hub** from https://unity.com/download
2. In Unity Hub → Installs → Add → select **Unity 6 (6000.0.x LTS)**
3. Add module: **Windows Build Support (IL2CPP)**
4. Open this repository as a Unity project (it will auto-import)

---

## Unity Personal License Activation (for CI)

Unity requires a valid license to build in CI. Follow the game-ci activation flow:

### Step 1 — Request a license file locally

```bash
# Run on a machine with Unity Editor installed
/path/to/Unity -batchmode -createManualActivationFile -logFile -
# Produces Unity_v6000.alf
```

Or use the game-ci GitHub Action:

```yaml
- uses: game-ci/unity-request-activation-file@v2
  # Produces Unity_v*.alf as an artifact
```

### Step 2 — Activate on Unity's website

1. Go to https://license.unity3d.com/manual
2. Upload the `.alf` file
3. Select **Unity Personal** license
4. Download the resulting `.ulf` file

### Step 3 — Add secrets to GitHub repository

In your repo: **Settings → Secrets and variables → Actions → New repository secret**

| Secret name | Value |
|---|---|
| `UNITY_LICENSE` | Full contents of the `.ulf` file |
| `UNITY_EMAIL` | Your Unity account email |
| `UNITY_PASSWORD` | Your Unity account password |

Reference: https://game.ci/docs/github/activation

---

## Local Build

1. Open the project in Unity 6 LTS
2. Configure the scene (see `Assets/Scenes/Main.unity` placeholder)
3. **File → Build Settings**
   - Platform: **PC, Mac & Linux Standalone**
   - Target Platform: **Windows**
   - Architecture: **x86_64**
4. **Player Settings** (see `ProjectSettings/SETTINGS_README.md`)
5. Click **Build**

---

## Triggering CI Build & Release

Push a version tag to trigger the GitHub Actions workflow:

```bash
git tag v0.1.0
git push origin v0.1.0
```

The workflow will:
1. Build `StandaloneWindows64` using `game-ci/unity-builder@v4`
2. Upload the build as a GitHub Actions artifact
3. Create a GitHub Release with the build attached

---

## Free 2D Pet Sprite Assets

| Source | Link | Notes |
|---|---|---|
| **itch.io** — Pixel Cat Sprites | https://elthen.itch.io/2d-pixel-art-cat-sprites | Free, CC0-style, multiple animations |
| **itch.io** — Tiny RPG Monster Pack | https://ansimuz.itch.io/tiny-rpg-monster | Free, various cute creatures |
| **itch.io** — Free Chibi Characters | https://luizmelo.itch.io/martial-hero | Free idle/walk/attack frames |
| **OpenGameArt** — Cute Animal Pack | https://opengameart.org/content/cute-animal-pack | CC0, multiple pets |
| **OpenGameArt** — Simple 2D Sprite Collection | https://opengameart.org/content/simple-2d-sprite-collection | CC0 |
| **Kenney.nl** — Animals Pack | https://kenney.nl/assets/animal-pack-redux | CC0, free commercial use |

> All CC0 assets can be used freely in personal and commercial projects without attribution.

---

## Architecture Notes

- `WindowManager.cs` uses `SetWindowLong` to strip the window border and enable `WS_EX_LAYERED`.
  `SetLayeredWindowAttributes` with colour key `#000000` punches through the black camera background, producing a transparent window without a separate alpha channel.
- `PetController.cs` drives a simple state machine. Mouse input is received via Unity's `Physics2DRaycaster` + `EventSystem` — no custom input polling.
- `AnimationController.cs` is data-driven: add any number of named `AnimClip` entries in the Inspector with sprite arrays and FPS.
